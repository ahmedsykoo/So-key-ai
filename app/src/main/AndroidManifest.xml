<?xml version="1.0" encoding="utf-8"?>
<!--
  So-key Ai — Android manifest.

  Package / applicationId: ai.sokey.workspace
  No library produced by an EAS/Shelly inheritance is referenced here: this is a
  standalone manifest for So-key Ai (see docs/BUILD_STATUS.md).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ai.sokey.workspace"
    android:versionCode="2"
    android:versionName="1.0.1">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="35" />

    <!-- AI providers, OmniRoute, GitHub, MCP/A2A over the network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- long running agents / automation -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- built-in APK installer tool: installing builds produced by the agent -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".SoKeyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:description="@string/app_description"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.SoKeyAi.Splash">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden|uiMode|locale|layoutDirection|fontScale|density"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SoKeyAi.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Second WebView host: the built-in browser / preview surface -->
        <activity
            android:name=".BrowserActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.SoKeyAi" />

        <!-- keeps agents/automation alive while the screen is off -->
        <service
            android:name=".AgentService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- shares generated builds / exports out of the sandbox -->
        <provider
            android:name=".ShareProvider"
            android:authorities="ai.sokey.workspace.files"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>
</manifest>
